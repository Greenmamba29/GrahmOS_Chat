[build]
  command = "bun run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Environment variables will need to be set in Netlify UI
# Required: OPENAI_API_KEY, TAVILY_API_KEY
